// DEPRECATED — replaced by app/app/Actions.tsx (the 4-step pipeline).
// Kept only because the sandbox can't delete it. Safe to remove on your machine:
//   rm app/app/Dashboard.tsx
export default function DeprecatedDashboard() {
  return null;
}
